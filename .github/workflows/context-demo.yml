name: Context variables demo
on: push

jobs:
  context-demo:
    runs-on: ubuntu-latest
  steps:
    - name: Use context varibales
      run:
        echo: "This worlflow is triggered by ${{github.actor}}"
        echo: "This repo name is ${{github.repository}}"
